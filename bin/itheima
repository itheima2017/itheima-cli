#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init', '创建新项目 - itheima init <template-name> <project-name>')
  .parse(process.argv)
